{
  "name": "subscription-payment-backend",
  "version": "1.0.0",
  "description": "Subscription payment system backend",
  "main": "server.ts",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only server.ts",
    "build": "tsc",
    "start": "node dist/server.js",
    "test": "jest",
    "setup": "ts-node scripts/setup.ts",
    "seed": "ts-node scripts/seedPlans.ts",
    "create-env": "ts-node scripts/createEnv.ts",
    "check-mongo": "ts-node scripts/checkMongoDB.ts",
    "postinstall": "npm run build || true"
  },
  "dependencies": {
    "express": "^4.18.2",
    "mongoose": "^7.5.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "jsonwebtoken": "^9.0.2",
    "axios": "^1.5.0",
    "bcryptjs": "^2.4.3"
  },
  "devDependencies": {
    "@types/express": "^4.17.17",
    "@types/node": "^20.5.0",
    "@types/cors": "^2.8.13",
    "@types/jsonwebtoken": "^9.0.2",
    "@types/bcryptjs": "^2.4.2",
    "typescript": "^5.1.6",
    "ts-node-dev": "^2.0.0"
  }
}

